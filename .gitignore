docs/clone/*
node_modules/
dist/
data/
.env

config/*
!config/config.example.json
!config/providers.example.json
!config/credentials.example.json
!config/groups.example.json
!config/cron.example.json

groups/*/

# 個人用 cron ハンドラー置き場。機構（src/cron/）とサンプル（jobs/*.ts）はコミットし、
# 個人ワークフロー固有のハンドラーはここに置いて git 管理外にする。
# src/ 配下なので tsc はコンパイルする（prod でも dist/ に .js が出る）。
src/cron/jobs/local/

.claude/plans/

__pycache__/
